name: yourinrolltoolsstream
description: Controlling the camera on Android and iOS plus rtmp live streaming support. Based off the Camera plugin Social Network Community Youinroll Flutter.
version: 1.1.1
homepage: https://github.com/Xatiko540/yourinrolltoolsstream

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=3.0.5"

dependencies:
  flutter:
    sdk: flutter
  rename: ^2.0.1

dev_dependencies:
  path_provider: ^2.0.5
  video_player: ^2.2.5
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  pedantic: ^1.8.0+1

# The following section is specific to Flutter.
flutter:
  plugin:
    platforms:
      android:
        package: com.app.rtmp_publisher
        pluginClass: YourinrolltoolsstreamPlugin
      ios:
        pluginClass: YourinrolltoolsstreamPlugin
